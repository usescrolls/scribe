stages:
  - prepare
  - test
  - build
  - publish
  - release

variables:
  GO_VERSION: "1.26"
  NODE_VERSION: "20"
  ZIG_VERSION: "0.14.1"
  GOMODCACHE: "$CI_PROJECT_DIR/.cache/go/pkg/mod"
  GOCACHE: "$CI_PROJECT_DIR/.cache/go/build"
  PNPM_STORE_DIR: "$CI_PROJECT_DIR/.cache/pnpm/store"
  CDN_PREFIX: "scribe"
  # Public base URL where binaries, icons, and release metadata are served.
  PUBLIC_DOWNLOAD_BASE: "https://cdn.usescrolls.com/scribe"

.go-cache: &go-cache
  cache:
    key:
      files:
        - go.sum
    paths:
      - .cache/go/build
      - .cache/go/pkg/mod

.pnpm-cache: &pnpm-cache
  cache:
    key:
      files:
        - frontend/pnpm-lock.yaml
    paths:
      - .cache/pnpm/store

# ---------------------------------------------------------------------------
# Prepare — build the desktop release image on the default branch
# ---------------------------------------------------------------------------
build-ci-image:
  stage: prepare
  image:
    name: gcr.io/kaniko-project/executor:v1.23.2-debug
    entrypoint: [""]
  script:
    - mkdir -p /kaniko/.docker
    - 'printf "{\"auths\":{\"%s\":{\"username\":\"%s\",\"password\":\"%s\"}}}" "$CI_REGISTRY" "$CI_REGISTRY_USER" "$CI_REGISTRY_PASSWORD" > /kaniko/.docker/config.json'
    - /kaniko/executor --context "${CI_PROJECT_DIR}" --dockerfile "${CI_PROJECT_DIR}/.gitlab/ci/desktop-build.Dockerfile" --build-arg GO_VERSION="${GO_VERSION}" --build-arg ZIG_VERSION="${ZIG_VERSION}" --destination "${CI_REGISTRY_IMAGE}/desktop-build:go${GO_VERSION}-zig${ZIG_VERSION}" --destination "${CI_REGISTRY_IMAGE}/desktop-build:main"
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      changes:
        - .gitlab-ci.yml
        - .gitlab/ci/desktop-build.Dockerfile
        - go.mod
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
      when: manual

# ---------------------------------------------------------------------------
# Test — Go coverage
# ---------------------------------------------------------------------------
go-test:
  stage: test
  image: golang:${GO_VERSION}
  <<: *go-cache
  script:
    - go test -v -coverprofile=coverage.out ./internal/...
    - go run github.com/boumenot/gocover-cobertura@v1.4.0 < coverage.out > coverage.xml
    - |
      GO_COV=$(go tool cover -func=coverage.out | awk '/^total:/ {gsub(/%/, "", $3); print $3}')
      echo "Go coverage: ${GO_COV}%"
      badge_color() {
        awk -v pct="$1" 'BEGIN {
          if (pct >= 90) print "brightgreen";
          else if (pct >= 80) print "green";
          else if (pct >= 70) print "yellowgreen";
          else if (pct >= 60) print "yellow";
          else if (pct >= 50) print "orange";
          else print "red";
        }'
      }
      mkdir -p badges
      GO_BADGE_COLOR=$(badge_color "$GO_COV")
      printf '{"schemaVersion":1,"label":"go coverage","message":"%s%%","color":"%s"}\n' "$GO_COV" "$GO_BADGE_COLOR" > badges/go-coverage.json
  coverage: '/Go coverage:\s+(\d+\.\d+)%/'
  artifacts:
    paths:
      - badges/go-coverage.json
      - coverage.out
      - coverage.xml
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG =~ /^v/

# ---------------------------------------------------------------------------
# Test — frontend tests and production bundle
# ---------------------------------------------------------------------------
frontend:
  stage: test
  image: node:${NODE_VERSION}-bookworm
  <<: *pnpm-cache
  before_script:
    - corepack enable
    - pnpm config set store-dir "${PNPM_STORE_DIR}"
    - pnpm --dir frontend install --frozen-lockfile --prefer-offline
  script:
    - pnpm --dir frontend run test:coverage
    - |
      FRONTEND_COV=$(node -e "const fs = require('node:fs'); const summary = JSON.parse(fs.readFileSync('frontend/coverage/coverage-summary.json', 'utf8')); process.stdout.write(summary.total.lines.pct.toFixed(1));")
      badge_color() {
        awk -v pct="$1" 'BEGIN {
          if (pct >= 90) print "brightgreen";
          else if (pct >= 80) print "green";
          else if (pct >= 70) print "yellowgreen";
          else if (pct >= 60) print "yellow";
          else if (pct >= 50) print "orange";
          else print "red";
        }'
      }
      mkdir -p badges
      FRONTEND_BADGE_COLOR=$(badge_color "$FRONTEND_COV")
      printf '{"schemaVersion":1,"label":"frontend coverage","message":"%s%%","color":"%s"}\n' "$FRONTEND_COV" "$FRONTEND_BADGE_COLOR" > badges/frontend-coverage.json
    - pnpm --dir frontend run build
  artifacts:
    paths:
      - badges/frontend-coverage.json
      - frontend/dist
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG =~ /^v/

# ---------------------------------------------------------------------------
# Build — Linux AMD64
# ---------------------------------------------------------------------------
build-linux-amd64:
  stage: build
  image: "$CI_REGISTRY_IMAGE/desktop-build:go${GO_VERSION}-zig${ZIG_VERSION}"
  <<: *go-cache
  needs:
    - job: go-test
      artifacts: false
    - job: frontend
      artifacts: true
  script:
    - 'if [ -n "${CDN_PREFIX}" ] && [ "${PUBLIC_DOWNLOAD_BASE%/}" != "${PUBLIC_DOWNLOAD_BASE%/${CDN_PREFIX}}" ]; then :; else echo "PUBLIC_DOWNLOAD_BASE must end with /${CDN_PREFIX}. Current value: ${PUBLIC_DOWNLOAD_BASE}" >&2; exit 1; fi'
    - VERSION=${CI_COMMIT_TAG#v}
    - CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -ldflags="-s -w -X gitlab.com/usescrolls/scribe/internal.Version=$VERSION -X gitlab.com/usescrolls/scribe/internal.PublicDownloadBase=$PUBLIC_DOWNLOAD_BASE" -o scribe-linux-amd64 .
  artifacts:
    paths:
      - scribe-linux-amd64
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/

# ---------------------------------------------------------------------------
# Build — Windows AMD64 (cross-compiled from Linux, no CGO)
# ---------------------------------------------------------------------------
build-windows-amd64:
  stage: build
  image: golang:${GO_VERSION}
  <<: *go-cache
  needs:
    - job: go-test
      artifacts: false
    - job: frontend
      artifacts: true
  script:
    - 'if [ -n "${CDN_PREFIX}" ] && [ "${PUBLIC_DOWNLOAD_BASE%/}" != "${PUBLIC_DOWNLOAD_BASE%/${CDN_PREFIX}}" ]; then :; else echo "PUBLIC_DOWNLOAD_BASE must end with /${CDN_PREFIX}. Current value: ${PUBLIC_DOWNLOAD_BASE}" >&2; exit 1; fi'
    - VERSION=${CI_COMMIT_TAG#v}
    - CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -ldflags="-s -w -H windowsgui -X gitlab.com/usescrolls/scribe/internal.Version=$VERSION -X gitlab.com/usescrolls/scribe/internal.PublicDownloadBase=$PUBLIC_DOWNLOAD_BASE" -o scribe-windows-amd64.exe .
  artifacts:
    paths:
      - scribe-windows-amd64.exe
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/

# ---------------------------------------------------------------------------
# Publish — upload Linux/Windows binaries and static assets to Cloudflare R2
# macOS is published locally from a tagged macOS checkout via
# scripts/release-macos-local.sh, which also writes the final latest manifest.
# ---------------------------------------------------------------------------
publish-cdn:
  stage: publish
  image: alpine:latest
  needs:
    - build-linux-amd64
    - build-windows-amd64
  before_script:
    - apk add --no-cache rclone
    - 'if [ -n "${CDN_PREFIX}" ] && [ "${PUBLIC_DOWNLOAD_BASE%/}" != "${PUBLIC_DOWNLOAD_BASE%/${CDN_PREFIX}}" ]; then :; else echo "PUBLIC_DOWNLOAD_BASE must end with /${CDN_PREFIX}. Current value: ${PUBLIC_DOWNLOAD_BASE}" >&2; exit 1; fi'
    - ': "${CDN_BUCKET:?CDN_BUCKET CI/CD variable is required. If it is protected, protect release tags (for example v*).}"'
    - ': "${R2_ACCESS_KEY_ID:?R2_ACCESS_KEY_ID CI/CD variable is required. If it is protected, protect release tags (for example v*).}"'
    - ': "${R2_SECRET_ACCESS_KEY:?R2_SECRET_ACCESS_KEY CI/CD variable is required. If it is protected, protect release tags (for example v*).}"'
    - ': "${R2_ENDPOINT:?R2_ENDPOINT CI/CD variable is required. If it is protected, protect release tags (for example v*).}"'
    - |
      cat > /tmp/rclone.conf <<EOF
      [r2]
      type = s3
      provider = Cloudflare
      access_key_id = ${R2_ACCESS_KEY_ID}
      secret_access_key = ${R2_SECRET_ACCESS_KEY}
      endpoint = ${R2_ENDPOINT}
      no_check_bucket = true
      EOF
    - export RCLONE_CONFIG=/tmp/rclone.conf
  script:
    - RELEASE_PREFIX="${CDN_PREFIX}/releases/${CI_COMMIT_TAG}"
    - rclone copyto scribe-linux-amd64 "r2:${CDN_BUCKET}/${RELEASE_PREFIX}/scribe-linux-amd64"
    - rclone copyto scribe-linux-amd64 "r2:${CDN_BUCKET}/${CDN_PREFIX}/scribe-linux-amd64"
    - rclone copyto scribe-windows-amd64.exe "r2:${CDN_BUCKET}/${RELEASE_PREFIX}/scribe-windows-amd64.exe"
    - rclone copyto scribe-windows-amd64.exe "r2:${CDN_BUCKET}/${CDN_PREFIX}/scribe-windows-amd64.exe"
    - rclone copyto icons/AppIcon.icns "r2:${CDN_BUCKET}/${CDN_PREFIX}/icons/AppIcon.icns"
    - rclone copyto icons/icon.png "r2:${CDN_BUCKET}/${CDN_PREFIX}/icons/icon.png"
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/

# ---------------------------------------------------------------------------
# Release — create GitLab release with changelog
# ---------------------------------------------------------------------------
release:
  stage: release
  image:
    name: registry.gitlab.com/gitlab-org/cli:latest
    entrypoint: [""]
  needs:
    - publish-cdn
  before_script:
    - apk add --no-cache git curl git-cliff
    - 'if [ -n "${CDN_PREFIX}" ] && [ "${PUBLIC_DOWNLOAD_BASE%/}" != "${PUBLIC_DOWNLOAD_BASE%/${CDN_PREFIX}}" ]; then :; else echo "PUBLIC_DOWNLOAD_BASE must end with /${CDN_PREFIX}. Current value: ${PUBLIC_DOWNLOAD_BASE}" >&2; exit 1; fi'
  script:
    - git fetch --tags
    - git cliff --latest --strip header > CHANGELOG.md
  release:
    tag_name: $CI_COMMIT_TAG
    name: $CI_COMMIT_TAG
    description: ./CHANGELOG.md
    assets:
      links:
        - name: scribe-linux-amd64
          url: "${PUBLIC_DOWNLOAD_BASE}/releases/${CI_COMMIT_TAG}/scribe-linux-amd64"
          filepath: /scribe/releases/${CI_COMMIT_TAG}/scribe-linux-amd64
        - name: scribe-windows-amd64.exe
          url: "${PUBLIC_DOWNLOAD_BASE}/releases/${CI_COMMIT_TAG}/scribe-windows-amd64.exe"
          filepath: /scribe/releases/${CI_COMMIT_TAG}/scribe-windows-amd64.exe
        - name: scribe-darwin-arm64
          url: "${PUBLIC_DOWNLOAD_BASE}/releases/${CI_COMMIT_TAG}/scribe-darwin-arm64"
          filepath: /scribe/releases/${CI_COMMIT_TAG}/scribe-darwin-arm64
  rules:
    - if: $CI_COMMIT_TAG =~ /^v/
